# Flow for handling cases where the agent cannot provide a relevant answer
# Copia archivada desde Rasa Studio: se mantiene solo como referencia.
flows:
  cannot_handle:
    description: Handle cases where no relevant answer is found
    if: False
    steps:
      - action: utter_no_relevant_answer_found
